from django.apps import AppConfig


class FlightTrainingConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.flight_training'
    label = 'flight_training'
    verbose_name = 'Flight Training'
