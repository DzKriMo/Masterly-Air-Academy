from django.apps import AppConfig


class GroundTrainingConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.ground_training'
    label = 'ground_training'
    verbose_name = 'Ground Training'
